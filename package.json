{
	"name": "nlevel",
	"description": "node.js odm for leveldb/levelup",
	"version": "1.0.1",
	"author": "Oleg Korobenko <oleg.korobenko@gmail.com>",
	"repository": {
		"type": "git",
		"url": "git://github.com/okv/nlevel"
	},
	"keywords": [
		"leveldb", "levelup", "level", "store", "storage", "db", "database",
		"in-process", "sqlight"
	],
	"scripts": {
		"test": "mocha test --reporter spec --bail --async-only",
		"benchmark": "mocha benchmark/*.bench.js --reporter spec --bail --async-only --slow 1 --timeout 30000",
		"doc": "dox --api < lib/index.js | sed -e 's/##/###/g'"
	},
	"dependencies": {
		"levelup": ">= 0.9.0"
	},
	"devDependencies": {
		"mocha": "1.11.0",
		"expect.js": "0.2.0",
		"dox": "0.4.4",
		"memdown": "1.0.0"
	},
	 "main": "./lib/index"
}
